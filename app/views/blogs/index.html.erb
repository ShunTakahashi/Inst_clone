<table class="index_table">
  <h1>投稿一覧</h1>
  <tbody>
  <% @blogs.each do |blog| %>

    <tr>
      <th class="show_td"><%= blog.content %></th>

      <td class="show_td">User:<%= blog.user.name %></td>

      <td class="show_td"><%= link_to '詳細', blog_path(blog.id),
                                      class: "btn btn-primary" %></td>
    </tr>
  <% end %>
  </tbody>
</table>
